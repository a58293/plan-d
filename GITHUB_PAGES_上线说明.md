# 绘屿造物官网 · GitHub Pages 上线说明

这份项目已经包含自动构建与发布流程。推荐使用“个人主页仓库”，这样首页、花神详情、防伪演示和法律说明的地址都能保持正常。

## 第一次上线

1. 登录 GitHub，新建一个 **Public** 仓库。
2. 仓库名称必须填写：`你的GitHub用户名.github.io`。例如用户名是 `lumen-auralis`，仓库名就是 `lumen-auralis.github.io`。
3. 解压收到的 GitHub 发布包，把解压后的全部内容上传到仓库根目录。需要连同隐藏的 `.github` 文件夹一起上传。
4. 打开仓库的 `Settings → Pages`，在 `Build and deployment` 的 `Source` 中选择 **GitHub Actions**。
5. 打开仓库的 `Actions` 页面，等待“发布绘屿造物官网”显示绿色对勾。
6. 网站地址为：`https://你的GitHub用户名.github.io/`。
7. 获得网站地址后，在腾讯云函数 `verify-authenticity` 的 `ALLOWED_ORIGINS` 环境变量中追加 `https://你的GitHub用户名.github.io`，与本地地址之间使用英文逗号分隔。

## 后续更新

- 替换图片：按 `素材库/替换图片说明.md` 操作，然后把修改后的文件重新上传。
- 修改文字或样式：更新 `src` 中对应文件并上传。
- 每次推送到 `main` 分支后，GitHub 会自动检查、构建并重新发布。

## 当前上线前仍需由品牌确认的内容

- 正式防伪后台与网站接口已经接通；真实发行防伪码尚未导入，因此正式公开前还需要完成防伪码表格与首批导入测试。
- 运营主体“绘屿造物”、客服 19988424290 与工作日 10:00—17:00 已写入；完整保养、售后、退换规则及条款生效日期仍待确认。
- 正式域名确认后再补分享封面、canonical（声明页面唯一正式地址）、sitemap（供搜索引擎发现页面的清单）并提交搜索引擎收录。
- 实拍图片与淘宝链接按现阶段计划后续补入，不影响先发布展示版。

## 当前性能策略

- 首屏等待条读取真实关键图片进度，不再用固定假进度。
- 5.8 MB 自定义字体不阻塞首屏，关键图片显示后再渐进载入。
- 同一浏览会话只播放一次完整品牌开场，页面之间切换不重复等待。
- 大图已改用无损 WebP；原 PNG 备份不放入 GitHub 发布包。

## 重要说明

- 不要上传 `node_modules`、`dist`、`素材管理`、`artifacts` 或旧的 `public/images` 历史图库；发布包中已经排除。
- 不要把客户订单、娃证数据、后台密码或任何密钥放进 GitHub 仓库。
- 如果不使用 `用户名.github.io` 作为仓库名，而使用普通项目仓库名，现有根路径路由需要另做子目录适配后再发布。
