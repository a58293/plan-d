# 图一单殿构图调整（已接入本地预览）

最新试版（2026-09-05）：用户要求先做一版统一美术与自然视差。选择页沿用角色页的深青、玉白文字与细金线；保留原人物与场景素材，展示层降低环境对比度，未覆盖原图。地板以同一注册背景的下部蒙版显示，并与人物、柱子放进同一个刚性舞台；远景/舞台/前景的最大水平位移分别为 2/6/16px，垂直为水平的四分之一，共用同一平滑信号。柱脚与地面不再独立滑动。触屏拖动仍只用于选人，减少动态效果设置下关闭视差。浏览器验证了舞台三部分位移一致、待启无入口、正常角色跳转、手机无横向溢出。下文为原接入阶段记录。

用户最新要求以参考图一为准，覆盖此前三席同时展示的构图：人物占满中央，建筑退至左右，右侧角色文案，底部名录仍可切换。保留四层视差与平光，镜昕原图未改。

已调整源代码与构建：单个全幅人物席位、其他席位离屏且不可聚焦；页面顶栏与名录叠在场景内；右侧增加真实角色入口；原有滑动、键盘、名录切换与防误点保留。

2026-09-05 已完成场景接入：左右两根玉石柱、浅浮雕柱础、梁枋和边缘薄纱；云海与远山、下方玉石地面；玉白荷花分布在近景两角。人物使用原图，完整等比显示。平光环境保留云层明暗与石材纹理，未增加人物背后的光束。柱脚、地面和裙摆重新对齐，人物高度与右侧文字间距按参考图调整。

柱廊原件和内置图像生成的一次透明提取结果均为 RGB 假棋盘格，没有作为透明素材发布。最终对已生成的建筑原件进行了本地连通背景提取，保留原件及 RGB 建筑内容，另存带真实 alpha 的 PNG；透明像素 1,094,977 个，中央 alpha 为 0。截图验证能透出实际云海和人物。处理原件、脚本和旧素材备份在工作区 `output/hall-finish/`。本地处理属于当前透明底接入工作，不改镜昕原图。

三个生成原件保存在工作区 `output/hero-scene-v2/`：`cloud-sea-floor.png`、`two-column-architecture.png`、`ivory-lotus-corners.png`，均为 1659 × 948。使用内置图像生成工具各生成一次，原件不改；完整提示词与素材检查见同目录 `prompts.md` 和 `qa.md`。

最终网页素材（相对项目根目录），三张均为 1659 × 948：

- `素材库/07_花神选择页/四层场景/02_中式石柱.png`：来自 `output/hall-finish/two-column-architecture-transparent.png`，真实透明建筑层。
- `素材库/07_花神选择页/四层场景/04_云海远景.png`：来自 `output/hero-scene-v2/cloud-sea-floor.png`，不透明云海与地面。
- `素材库/07_花神选择页/四层场景/01_镜昕代表花_荷花.png`：来自 `output/hero-scene-v2/ivory-lotus-corners.png`，真实透明荷花；网页缩小并轻微虚化展示。

素材由内置图像生成工具制作，建筑透明底随后本地处理；未使用 CLI 图像生成。完整原始提示词见 `四层殿堂素材提示词.md`，此次透明提取工具提示词见 `柱廊透明提取提示词.txt`。后者的出图未被采用。

正常构建通过。桌面 1664 × 941 与手机 390 × 844 检查完成；实际鼠标移动观测到从云海至花卉逐层增大的位移；待启状态无详情入口，选择镜昕后入口可进入正确地址，手机无横向溢出，浏览器无脚本错误。效果见工作区 `output/hall-finish/desktop.png` 与 `mobile.png`。图像生成的纹理与参考存在差异，不代表逐像素复刻。

本次不涉及首页新挂画、防伪数据库、域名或上线。本地预览地址：`http://localhost:3000/series/flower-gods`。
