# 序章界面简化与重看

本包是已上线“轻量无缝定格版”的界面补丁，不需要重新上传视频、音乐和海报。合并包内 src 文件后重新部署，保留当前媒体文件、发布白名单和安全配置。未修改域名、GitHub Actions 或购买网址，未创建定时任务。

- 删除“本期序章”的准备卡片、说明文案和“开启本期”按钮。
- 加载期间仅三个细小光点，仍可跳过。维持先缓冲 12 秒且海报解码完成再播放的门槛。
- 准备好后直接静音自动播放；右上角“开启声音”用于解除静音。浏览器限制有声自动播放，因此不能同时取消交互并保证自动出声。
- 若浏览器拒绝静音自动播放，显示轻量的圆形播放按钮；后台准备完成时也不会偷偷播放。
- 暂停/失败时仍有继续或重试，未恢复旧的卡片。
- 手机首页标题区和电脑首页入口区加入“重看本期序章”。无须清除已看记录，重看从零开始；退出返回原页面位置并把键盘焦点交还按钮。
- 保留第 10 秒的购买/进入主页按钮、同一期仅首次自动展示、曲终停留及最后一帧衔接。

本地类型检查、生产构建、Edge 手机尺寸/电脑尺寸自动测试通过；测试覆盖静音自动播放、无准备卡片、点击开声、跳过、按钮可见、从零重看、焦点恢复、刷新不自动重复。iOS/安卓微信真机仍需部署后检查。
