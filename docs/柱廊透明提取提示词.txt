Use case: background-extraction
Asset type: transparent PNG architectural overlay for a website.
Input images: Image 1 is the EDIT TARGET. Its white/light-gray checkerboard is painted into an RGB background; it is NOT actual transparency.
Primary request: Remove ONLY every white/light-gray checkerboard background region and replace those background pixels with genuine transparent alpha (alpha=0). The output must be an actual RGBA PNG with a transparent background, not a picture of a checkerboard. Remove the checkerboard from the large central opening, the small gaps in the carved top lattice, and the exposed space outside the columns and beneath their bases.
Preserve EXACTLY the existing artwork, composition, dimensions/aspect ratio, proportions, positions, perspective, and fine details of the two pale jade Chinese columns with lotus relief bases, jade top beam and carved brackets, and the two silky jade curtains along the side edges. Preserve their existing fine realistic carved stone and fabric textures, pale jade color, and even flat lighting. Keep all foreground opacity and detail intact, with clean antialiased cutout edges.
Constraints: This is background extraction only. Do not redraw or restyle the architecture, add objects, change placement, crop, or alter the camera. No people, floor, clouds, scenery, UI, lettering, watermark, or rendered checkerboard. The entire opening must be truly transparent so another image will show through it when layered.
